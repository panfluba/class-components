export interface Item {
  id: string;
  image: string;
  title: string;
  description: string;
  // types: number[];
  // sizes: number[];
  // price: number;
  // category: number;
  // rating: number;
}
